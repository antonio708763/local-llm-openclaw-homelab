# Power Usability, Context, and Memory Checkpoint

## Purpose

This checkpoint records the August 11, 2026 transition from "make Power technically work" to "make Power comfortable enough to use as a normal long-lived local assistant."

The target experience is straightforward:

- talk to an uncensored local model normally;
- keep visible model thinking off unless deliberately enabled;
- give the assistant tools only when it needs them instead of spending context on every tool schema up front;
- keep durable long-term memory outside the live model context;
- retain searchable session history;
- let Power act on deliberately connected computers through OpenClaw nodes;
- preserve long conversations through compaction and retrieval instead of repeatedly starting over.

The architecture goal is therefore:

```text
Live model context   = working memory
OpenClaw memory      = durable long-term memory
Session history      = searchable archive
Tool Search          = load tools only when needed
OpenClaw nodes       = hands on connected devices
Compaction           = keep long conversations sustainable
```

## Current Power model

The original Power alias remains available:

```text
qwen3-abliterated:30b
```

It was created from:

```text
hf.co/mradermacher/Qwen3-30B-A3B-abliterated-erotic-i1-GGUF:Q4_K_M
```

A second derived Ollama model is now the normal Power default:

```text
qwen3-abliterated-nothink:30b
```

The original alias is intentionally retained as a rollback/reference model.

## Why `thinking: false` was not enough

OpenClaw already reported the Power model with provider-level thinking/reasoning disabled, and direct Ollama requests were also tested with:

```json
{
  "think": false
}
```

Despite that, the original model could still emit a large visible reasoning block inside normal assistant content.

The important discovery was that the Ollama model template itself prefilled an opening `<think>` tag for the final assistant turn. The relevant end-of-template structure included a final assistant prefill equivalent to:

```text
{{- if and (ne .Role "assistant") $last }}<|im_start|>assistant
<think>
{{- end }}
```

That is different from merely supporting a separate Ollama `thinking` field. The template was nudging the model to begin its answer inside a thinking block even when higher layers said thinking was off.

## No-think model fix

The generated Modelfile for `qwen3-abliterated:30b` was copied to a temporary file. Only the final forced-thinking prefill was changed.

The new final prefill uses an immediately closed block:

```text
{{- if and (ne .Role "assistant") $last }}<|im_start|>assistant
<think></think>
{{- end }}
```

The historical-message template logic that can render an actual `.Thinking` value was left intact. This preserves compatibility with existing message structures while stopping the final blank assistant turn from being forced into visible reasoning.

The derived model was then created with:

```bash
ollama create qwen3-abliterated-nothink:30b \
  -f /tmp/qwen3-abliterated-nothink.modelfile
```

Validation showed the new alias in `ollama list`, using the same existing model layer rather than duplicating the full GGUF payload.

## Direct Ollama validation

A direct `/api/chat` request to the new alias was tested with an exact-response prompt.

Observed result:

```text
THINKING: None
CONTENT:  'NOTHINK_TEST_PASS'
```

A second arithmetic test also returned:

```text
THINKING: None
CONTENT:  '42'
```

This is the key proof that the new model path no longer emits the unwanted reasoning block before the final answer.

`ollama ps` also showed the no-think model running at:

```text
Context:    32768
Processor:  100% GPU
Loaded:     about 21 GB
```

## OpenClaw registration

The Power Ollama provider now contains a model entry for:

```text
qwen3-abliterated-nothink:30b
```

with the intended behavior:

```json
{
  "reasoning": false,
  "contextWindow": 32768,
  "maxTokens": 8192,
  "params": {
    "num_ctx": 32768,
    "thinking": false
  }
}
```

The Power agent model allow/configured set now contains both:

```text
ollama/qwen3-abliterated:30b
ollama/qwen3-abliterated-nothink:30b
```

The normal Power default is now:

```text
ollama/qwen3-abliterated-nothink:30b
```

A direct OpenClaw model-run test returned exactly:

```text
OPENCLAW_NOTHINK_PASS
```

The browser `/model status` view also confirmed that both `Current` and `Default` were the no-think model.

## OpenClaw thinking defaults

Power also has these agent defaults set:

```text
agents.defaults.thinkingDefault = off
agents.defaults.reasoningDefault = off
```

A fresh Power session reported:

```text
Think: off
Fast: off
```

The model-level no-think fix is still important because the previous visible reasoning was generated by the model template itself rather than only by OpenClaw's reasoning-visibility setting.

## Compaction configuration

The currently validated Power compaction configuration is:

```json
{
  "mode": "default",
  "reserveTokens": 8192,
  "keepRecentTokens": 6000,
  "reserveTokensFloor": 8192,
  "midTurnPrecheck": {
    "enabled": true
  }
}
```

The 32,768-token window is therefore deliberately reserving 8,192 tokens for response/tool/runtime headroom while keeping roughly 6,000 recent tokens verbatim when older history must be compacted.

## Context-pruning changes

The Power profile was also tuned with:

```text
agents.defaults.contextInjection = continuation-skip
agents.defaults.contextPruning.mode = cache-ttl
agents.defaults.contextPruning.ttl = 5m
skills.limits.maxSkillsPromptChars = 2500
```

A configuration backup was created before the context-optimization work:

```text
~/.openclaw-power/openclaw.json.before-context-optimization-20260811-004536.bak
```

The Gateway was restarted after validation and returned HTTP 200 on `127.0.0.1:19789`.

## Context measurements after optimization

The no-think model fixed reasoning spam, but it exposed the next bottleneck: the live Power prompt is still expensive before a real conversation has done much work.

A fresh test session with only a handful of short messages reported roughly:

```text
Context: 13k / 33k (about 41%)
```

Detailed context inspection showed approximately:

```text
Tracked prompt estimate:      about 47k-51k chars
Tracked prompt tokens:        about 11.8k-12.7k
Actual cached context:        about 13.4k tokens
Untracked provider/runtime:   about 1.6k tokens
System prompt run:            about 26.4k chars / 6.6k tokens
Project context:              about 2.95k tokens
Tool schemas JSON:            about 20,985 chars / 5,247 tokens
```

The tool schemas are therefore one of the largest avoidable consumers of the 32K working window.

## Largest tool-schema consumers observed

The detailed context view showed these approximate schema costs:

```text
cron               9,606 chars   ~2,402 tokens
skill_workshop      2,298 chars   ~575 tokens
exec                1,332 chars   ~333 tokens
sessions_spawn      1,151 chars   ~288 tokens
process             1,074 chars   ~269 tokens
web_search            991 chars   ~248 tokens
```

Several smaller tools then add additional hundreds of tokens collectively.

This explains why a fresh conversation can already be near 40% of a 32K window before useful work has accumulated.

## Skills optimization result

The skills prompt was reduced substantially.

Earlier context inspection showed approximately:

```text
17 skills
skills list prompt: about 5,958 chars / 1,490 tokens
```

After setting:

```text
skills.limits.maxSkillsPromptChars = 2500
```

a later fresh session showed approximately:

```text
10 skills
skills list prompt: about 2,443 chars / 611 tokens
```

That is a meaningful improvement, but tool JSON schemas remain the larger baseline cost.

## Important conclusion: do not solve this only by increasing context

Moving directly from 32K to 64K would provide more breathing room, but it would also allow the same oversized startup prompt to occupy a larger working set and increase KV-cache/VRAM pressure.

The better order is:

```text
1. Stop unwanted thinking output.              DONE
2. Reduce always-injected skills.              DONE / improved
3. Prune stale context.                        DONE / configured
4. Load large tools only when needed.          NEXT
5. Build durable retrieval-based memory.       NEXT
6. Measure normal multi-turn behavior again.
7. Only then test 64K context if useful.
```

## Next priority: Tool Search

The next Power milestone is to configure OpenClaw so the model does not receive every large tool schema on every turn.

The desired behavior is conceptually:

```text
User asks a normal question
    -> no remote/system/cron schema tax unless needed

User asks to change a file
    -> retrieve/load the relevant file/edit tools

User asks to control Alienware
    -> retrieve/load node/system tools

User asks for scheduled work
    -> retrieve/load cron tooling
```

This should recover several thousand tokens of working context in ordinary chats while preserving the same capabilities on demand.

## Long-term memory design after Tool Search

The intended memory architecture is not "keep every fact forever in the 32K prompt."

Instead:

```text
Stable personal/project facts
    -> durable memory notes / structured memory

Detailed old conversations
    -> session archive

Relevant older facts
    -> retrieval into the current turn only when useful

Recent conversation
    -> live context

Old live conversation
    -> compaction summary
```

This makes the model's context behave more like a desk and long-term memory more like a filing cabinet. The desk should contain what is being worked on now, not every document ever created.

A later milestone should add a local semantic embedding/retrieval path so Power can search a larger personal/project memory archive without requiring an OpenAI API key.

## Remote-device status

`Alienware-15-R3` remains the first Power node and is correctly:

```text
paired
connected
approved
```

with capabilities:

```text
browser
file
local-inference
system
```

Advertised system commands include:

```text
system.execApprovals.get
system.execApprovals.set
system.run
system.run.prepare
system.which
```

The stale-pairing recovery procedure is documented separately in `docs/20-alienware-power-node.md` and should be used before changing random exec settings if the node later reconnects with empty capabilities.

The command-preflight interface also requires the correct argument shape. In this OpenClaw build, `system.which` expects a `bins` array, for example:

```bash
openclaw --profile power nodes invoke \
  --node Alienware-15-R3 \
  --command system.which \
  --params '{"bins":["hostname","whoami","bash"]}'
```

A successful test resolved:

```text
hostname -> /usr/bin/hostname
whoami   -> /usr/bin/whoami
bash     -> /usr/bin/bash
```

End-to-end ordinary command execution from the browser should still be treated as a validation item until repeated harmless commands succeed consistently under the final approval policy.

## Desired final Power experience

The target is intentionally simpler than the implementation underneath it:

```text
Open OpenClaw Power
        |
        v
Talk normally to qwen3-abliterated-nothink:30b
        |
        +--> remembers durable facts through retrieval
        |
        +--> loads tools only when relevant
        |
        +--> acts on approved connected nodes
        |
        +--> compacts old conversation automatically
        |
        +--> rarely requires a new chat because of context pressure
```

The infrastructure should disappear behind the conversation instead of becoming the conversation.

## Next work queue

1. Configure and validate Tool Search / on-demand tool loading.
2. Re-measure `/status` and `/context detail` in a fresh session.
3. Aim to cut the fresh-session baseline substantially below the current ~13K tokens.
4. Build persistent long-term memory for the Power workspace.
5. Add local semantic retrieval/search over durable memory and older notes.
6. Define automatic memory-capture behavior so useful stable facts are saved without dumping every chat into the live prompt.
7. Finish repeated harmless Alienware execution validation and make the node process persistent.
8. Define normal device-routing conventions for additional connected computers.
9. Define explicit confirmation rules for genuinely high-impact actions plus a simple emergency stop.
10. After baseline context is lean, test 64K context and measure RTX 4090 VRAM/performance impact.
11. Later evaluate Qwen3.6-27B.
12. Later benchmark and possibly migrate from Ollama to standalone `llama.cpp` while preserving a rollback path.

## Resume point

The next technical task is **Tool Search**, not another model change and not another context increase.

Before changing anything, confirm the known-good Power baseline:

```bash
openclaw --profile power models status
openclaw --profile power config get agents.defaults.thinkingDefault
openclaw --profile power config get agents.defaults.reasoningDefault
openclaw --profile power config get agents.defaults.compaction --json
openclaw --profile power config get agents.defaults.contextPruning --json
openclaw --profile power nodes describe --node Alienware-15-R3
systemctl --user is-active openclaw-gateway-power.service
curl -fsSI http://127.0.0.1:19789/ | head -n 1
```

Expected model:

```text
ollama/qwen3-abliterated-nothink:30b
```

Expected Gateway probe:

```text
HTTP/1.1 200 OK
```
